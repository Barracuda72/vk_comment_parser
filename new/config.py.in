#!/usr/bin/env false

class rabbitmq(object):
    username = "guest"
    password = "guest"
    queue = "queue_name"

class postgres(object):
    username = "username"
    password = "password"
    database = "database"

#!/usr/bin/env false

class rabbitmq(object):
    host = "localhost"
    username = "guest"
    password = "guest"
    queue = "queue_name"

class postgres(object):
    username = "username"
    password = "password"
    database = "database"

#!/usr/bin/env false

from datetime import timedelta

class rabbitmq(object):
    host = "localhost"
    username = "guest"
    password = "guest"
    work_queue = "work_queue_name"
    login_queue = "login_queue_name"

class postgres(object):
    username = "username"
    password = "password"
    database = "database"

class collector(object):
    depth = 1
    max_photo_count = 100
    max_post_count = 100
    max_comment_count = 100
    user_time_delta = timedelta(days=90)
